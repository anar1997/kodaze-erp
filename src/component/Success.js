import React from 'react'

const Success = ({message}) => {
  return (
    <div style={{padding: 15, fontSize: 16, backgroundColor: "green", color: "white"}}>{message}</div>
  )
}

export default Success